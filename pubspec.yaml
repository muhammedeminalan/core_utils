name: wonzy_core_utils
description: "Flutter/Dart extensions and reusable widgets for rapid UI development. Context helpers, navigation shortcuts, text builders, layout extensions, and customizable widgets."
version: 0.2.1
homepage: https://github.com/muhammedeminalan/core_utils
repository: https://github.com/muhammedeminalan/core_utils
issue_tracker: https://github.com/muhammedeminalan/core_utils/issues
topics:
  - extensions
  - widgets
  - ui
  - utilities

environment:
  sdk: ^3.11.0
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  # ──────────── Form ────────────
  flutter_form_builder: ^10.3.0+2
  form_builder_validators: ^11.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
